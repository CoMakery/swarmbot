machine:
  node:
    version: 4

deployment:
  edge:
    branch: master
    commands:
      - bin/deploy edge swarmbot-edge
  staging:
    branch: staging
    commands:
      - bin/deploy staging swarmbot-staging
