machine:
  node:
    version: 5.3.0
test:
  override:
    - npm run fulltest
deployment:
  edge:
    branch: master
    commands:
      - bin/deploy $HEROKU_INSTANCES_EDGE
  staging:
    branch: staging
    commands:
      - bin/deploy $HEROKU_INSTANCES_STAGING
