machine:
  node:
    version: 5.3.0
  post:
    - npm install -g npm@3.5.x
test:
  override:
    - npm run fulltest
deployment:
  edge:
    branch: master
    commands:
      - bin/deploy swarmbot-edge
  staging:
    branch: staging
    commands:
      - bin/deploy swarmbot-staging
  production:
    branch: production
    commands:
      - bin/deploy $HEROKU_INSTANCES_PRODUCTION
