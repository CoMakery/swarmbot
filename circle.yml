machine:
  node:
    version: 5.3.0
  post:
    - npm install -g npm@3.5.x
test:
  override:
    - npm run fulltest
deployment:
  edge:
    branch: master
    commands:
      - bin/deploy edge swarmbot-edge
  staging:
    branch: staging
    commands:
      - bin/deploy staging swarmbot-staging
