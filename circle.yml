machine:
  node:
    version: 4.2.3
  post:
    - npm install -g npm@3.5.2
test:
  override:
    - npm run fulltest
deployment:
  edge:
    branch: master
    commands:
      - bin/deploy edge swarmbot-edge
  staging:
    branch: staging
    commands:
      - bin/deploy staging swarmbot-staging
